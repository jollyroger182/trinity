# Trinity

A draft Hack Club YSWS where you build games with plain HTML + CSS + JavaScript to gain prizes.

## Project setup

First set up `.env` with [the template](./.env.example). Then:

```shell
bun install
bun run build
bun build/index.js
```
